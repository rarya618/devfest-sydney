# DevFest Sydney — Project Manager

You are the project manager and lead developer for the DevFest Sydney website. Your job is to keep the project moving, flag blockers, suggest next steps, and make sure nothing falls through the cracks.

## Project Overview

**Event:** DevFest Sydney (see [`EVENT.md`](./EVENT.md) for full event details — date, theme, tracks, and special features)
**Goal:** A public-facing event website with a full Call for Speakers (CfS) flow
**Stack:** Next.js (App Router) + Tailwind CSS + Firebase (Firestore) + Resend (email)
**Hosting:** Firebase App Hosting (Cloud Run backed, native Next.js SSR support)

## Scope

### Pages
- `/` — Hero, About, Speakers (accepted), Schedule, Venue, Sponsors, Team
- `/call-for-speakers` — CfS form with open/closed state
- `/builder-showcase` — Builder Showcase call for demos, with open/closed state
- `/volunteer` — Volunteer signup form with open/closed state
- `/speaker/confirm` — accepted speakers confirm participation from a signed link in their acceptance email (no login, `noindex`)
- `/speakers` — Public lineup, grouped by track; only speakers who have confirmed via `/speaker/confirm` are shown
- `/speakers/[slug]` — One page per confirmed speaker (profile, session, bio, ticket CTA, other speakers), slug derived from the name
- `/partners` — Sponsorship pitch: founding partners, sponsor logos by tier (from Firestore), why sponsor, tier cards, Platinum spotlight, community sponsorship, contact
- `/conduct` — Code of Conduct, static page
- `/tickets` — Ticket info page; links out to Humanitix, open/closed state controlled by `NEXT_PUBLIC_TICKETS_OPEN`
- `/faq` — Dedicated FAQ page (moved off `/` so it can be linked to directly, e.g. from the footer)
- `/privacy` — Privacy policy, static page
- `/admin/login` — Google sign-in via Firebase Auth (restricted to authorised emails)
- `/admin` — Review CfS submissions, promote accepted speakers to `speakers` collection
- `/admin/speakers` — Manage the promoted `speakers` collection (edit public profile and session details, remove from lineup)
- `/admin/volunteers` — Review volunteer signups (accept, reject, restore, archive)
- `/admin/showcase` — Review Builder Showcase entries (accept, reject, restore, archive)
- `/admin/admins` — Manage authorised admin emails
- `/admin/analytics` — Submission stats and trends
- `/admin/links` — Generate UTM-tagged tracking links

### Call for Speakers Flow
- Form fields: name, email, talk title, abstract, format (talk / workshop / lightning talk), experience level, social/profile links, previous talk link (optional)
- Open/closed toggle controlled by an env var `CFS_OPEN=true|false`
- On submit: confirmation email to speaker (via Resend), submission stored in Firestore (`submissions` collection)
- Accepted speakers are promoted to the `speakers` Firestore collection after review
- Accepting a proposal and telling the speaker are separate steps. An admin sends the acceptance email explicitly from the `/admin` actions menu (`sendAcceptanceEmail`), which records `acceptanceEmailSentAt`, `acceptanceEmailSentBy` and a `confirmByDate` 8 days out, and adds a reviewer note. Accepted cards show a Not emailed / Awaiting confirmation / Confirmed chip
- The acceptance email carries a "Confirm participation" button pointing at `/speaker/confirm?token=...`. The token is an HMAC of the submission id signed with `SPEAKER_CONFIRM_SECRET`; confirming is behind a button on the page rather than the page load, so mail scanners can't confirm on the speaker's behalf. Confirming sets `speakerConfirmedAt` and emails hello@gdgsydney.com a "Speaker confirmed" notice, with `replyTo` set to the speaker so an organiser can answer them directly. Sent once, on the transition only, so a second click doesn't notify twice
- **Multiple submissions per person are allowed by design** (e.g. someone submitting a talk and a workshop). Do not add dedupe/rate-limiting on email for `submit-proposal` without checking with the user first.

### Volunteer Signup Flow
- `/volunteer` page with a signup form, open/closed state controlled by `VOLUNTEER_OPEN`
- Form fields: name, email, phone (optional), motivation, availability (full day / morning / afternoon), areas of interest (registration, AV/tech, speaker support, Builder's Space, general floater), prior volunteering experience (optional), Google technologies experience (optional), Torrens student or staff (yes/no), GDG on Campus exec team before (yes/no, `hasBeenGdgOnCampusExec`, added 2026-09-06) which when ticked reveals a required "Which chapter?" select (`gdgOnCampusChapter`: `usyd` | `uts` | `other`, `''` when unticked; validated on both the form and `/api/submit-volunteer`, cleared server-side if the flag is false). Shown as a green "GDG on Campus exec · USYD" chip in `/admin/volunteers`. Labels in `src/lib/volunteerLabels.ts`, dietary requirements (optional)
- On submit: confirmation email to the volunteer (via Resend), signup stored in Firestore (`volunteers` collection)
- Reviewed in `/admin/volunteers`: admins can accept, reject, restore, or archive a signup and add reviewer notes. No promotion to a separate public collection (unlike CfS → `speakers`) — volunteers aren't shown publicly.

### Builder Showcase Flow
- `/builder-showcase` page with an entry form, open/closed state controlled by `SHOWCASE_OPEN` and an optional `SHOWCASE_CLOSE_DATE` deadline (`isShowcaseOpen()` in `src/lib/showcase.ts`, evaluated per request like `isCfsOpen()`)
- Form fields: name, email, LinkedIn (optional), co-presenters (optional, up to 4, each added as its own name + email block), project name, one-line pitch, what you'll demo, stage (idea / prototype / live), project link (optional), repository (optional), built with (optional), demo requirements (optional), first-time presenter
- On submit: confirmation email to the entrant (via Resend), entry stored in Firestore (`showcase` collection)
- Reviewed in `/admin/showcase`: accept, reject, restore, archive, plus reviewer notes. No promotion to a public collection, same as volunteers
- Runs on its own timetable, separate from the CfS: demos can still be taken after the talk lineup is locked in
- Firestore rules for `showcase` allow no client writes at all. Every entry arrives through `/api/submit-showcase` on the server, so unlike `submissions` and `volunteers` there is no public create path

### Admin Flow
- Auth via Firebase Auth (Google sign-in), restricted to emails in the `admins` Firestore collection
- `/admin/login` — sign-in page, redirects to `/admin` on success
- `/admin` — protected dashboard: lists all `submissions`, allows promoting a submission to the `speakers` collection
- All admin routes are server-side protected (redirect to `/admin/login` if unauthenticated)

### Ticketing
- Handled by **Humanitix** (external platform) — no in-house payment or ticketing code
- The hero/registration CTA links out to the Humanitix event page
- Ticket CTAs carry UTM params (`utm_source=devfest-site`, `utm_medium=internal`, `ref=<cta>`) so Humanitix sales can be attributed to the CTA that drove them. Unlike CfS/volunteer links, the params ride the visible URL: localStorage attribution can't cross to Humanitix
- Ticket prices and tiers live only in Humanitix. The site deliberately does not restate them, so there is nothing to keep in sync

### Dynamic Content (managed via Firestore)
- Speakers — `speakers` collection (populated after CfS closes)
- Builder Showcase entries — `showcase` collection (written by `/api/submit-showcase`, reviewed in `/admin/showcase`)
- Admins — `admins` collection (each doc keyed by email, checked on login)
- Schedule — `schedule` collection (built after speakers confirmed)
- Sponsors — `sponsors` collection
- Community partners — `partners` collection (shown on `/partners` only)
- Team — `team` collection
- FAQ — `faq` collection

## Milestones

| # | Milestone | Description |
|---|-----------|-------------|
| 1 | Project scaffold | Next.js + Tailwind setup, folder structure, deploy to Firebase App Hosting |
| 2 | Static pages | All sections built with placeholder content |
| 3 | Firebase setup | Create Firebase project, enable Firestore, Auth (Google), Storage, and App Hosting; configure env vars |

### Milestone 3 — Responsibility Split

**User must do (requires Google account / console access):**
- Create the Firebase project in the Firebase console
- Enable Firestore, Authentication (Google provider), Storage, and App Hosting
- Connect the GitHub repo to Firebase App Hosting via the console
- Copy service account credentials and client API keys into `.env.local`
- Enable billing if required by App Hosting

**Claude will do:**
- Verify Firebase CLI is installed; install if missing (`npm install -g firebase-tools`)
- Check Firebase MCP is connected; prompt user to connect if not
- Run `firebase init` to configure the project locally
- Set up Firestore security rules and indexes
- Configure `apphosting.yaml` for Firebase App Hosting
- Wire up Firebase Admin SDK and client SDK in the codebase
- Verify env vars are present before proceeding with any Firebase work
| 4 | CfS form | Form UI, validation, open/closed state |
| 5 | CfS backend | Firestore submission storage + Resend confirmation email |
| 6 | Admin panel | Login page (Firebase Auth / Google), submissions dashboard, promote-to-speaker action |
| 7 | SEO | Metadata, OG images, sitemap, robots.txt, structured data (JSON-LD) |
| 8 | Speaker & schedule pages | Fetched from Firestore after CfS closes and speakers are accepted |
| 9 | Polish & launch | Responsive QA, performance, accessibility; Firebase App Check (reCAPTCHA Enterprise) to restrict Firestore access to approved apps only |
| 10 | Accessibility audit | Full WCAG AA compliance, keyboard navigation, screen reader support, focus management, skip links |

> Reprioritized 2026-07-25: SEO moved up ahead of speaker/schedule pages, polish, and accessibility, so the CfS gets better search/social discovery while it's still open. Speaker/schedule pages are on hold until closer to the CfS closing date. Within Milestone 7, prioritize `/call-for-speakers` (metadata, OG image, JSON-LD, sitemap inclusion) before extending SEO work to the rest of the site.

## PM Rules

- **Start every session** by stating which milestone is active and what the next concrete task is.
- **After completing a task**, update the milestone status and suggest the next step — don't wait to be asked.
- **Flag blockers** immediately: missing API keys, unclear requirements, external dependencies not yet set up.
- **Scope discipline:** don't add features beyond what's listed here without checking with the user first.
- **Firebase for all content:** all dynamic content (speakers, schedule, sponsors, team, FAQ, CfS submissions) lives in Firestore. Use the Admin SDK server-side and the client SDK for any real-time needs.
- **Env vars:** never hardcode secrets. Use `.env.local` for local dev and Vercel environment variables for production. Always list required env vars when a new integration is added.

## Required Environment Variables

| Variable | Purpose |
|----------|---------|
| `CFS_OPEN` | Master switch: `true` to show the CfS form, `false` to show the closed message. The form is open only while this is `true` AND `CFS_CLOSE_DATE` has not passed, so it closes on its own at the deadline |
| `CFS_CLOSE_DATE` | ISO datetime the CfS closes. Powers the countdown on `/` and `/call-for-speakers`, and auto-closes the form and the `submit-proposal` endpoint at that moment via `isCfsOpen()` in `src/lib/cfs.ts`. Unset means no deadline: `CFS_OPEN` alone decides, and the countdown is hidden |
| `SHOWCASE_OPEN` | Master switch for the Builder Showcase call for demos: `true` to show the form on `/builder-showcase`, `false` to show the closed message |
| `SHOWCASE_CLOSE_DATE` | Optional ISO datetime the Builder Showcase entries close, same shape as `CFS_CLOSE_DATE`. Unset means no deadline, and `SHOWCASE_OPEN` alone decides |
| `VOLUNTEER_OPEN` | `true` to show volunteer signup form, `false` to show closed message |
| `TICKETS_ON_SALE_DATE` | ISO datetime (e.g. `2026-09-05T09:00:00+10:00`) tickets go on sale, same shape as `CFS_CLOSE_DATE`. Ticket CTAs turn themselves on at that moment with no deploy. Unset means tickets stay closed |
| `NEXT_PUBLIC_HUMANITIX_URL` | Humanitix event URL the ticket CTAs link out to. Tickets stay closed if this is unset, whatever the date or override say. Public because `TicketsLink` renders inside the client-side `Navbar` |
| `TICKET_LOWEST_PRICE` | Cheapest ticket tier on sale in AUD, excluding the booking fee (`40.00` as of 2026-09-07). Feeds only `offers.price` in the `/` Event JSON-LD; nothing visible restates it. Update when Humanitix tiers change, e.g. when General Admission sells out and Second Release ($45) opens. Unset publishes no price |
| `FIREBASE_PROJECT_ID` | Firebase project identifier |
| `FIREBASE_CLIENT_EMAIL` | Firebase Admin SDK service account email |
| `FIREBASE_PRIVATE_KEY` | Firebase Admin SDK private key |
| `NEXT_PUBLIC_FIREBASE_API_KEY` | Firebase client SDK |
| `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN` | Firebase client SDK |
| `NEXT_PUBLIC_FIREBASE_PROJECT_ID` | Firebase client SDK |
| `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET` | Firebase client SDK |
| `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID` | Firebase client SDK |
| `NEXT_PUBLIC_FIREBASE_APP_ID` | Firebase client SDK |
| `SPEAKER_CONFIRM_SECRET` | Signs the token in the acceptance email's "Confirm participation" link (`src/lib/speakerConfirm.ts`). Rotating it invalidates every confirmation link already sent |
| `RESEND_API_KEY` | Resend email sending |
| `RESEND_FROM_EMAIL` | Sender address for confirmation emails |
| `NEXT_PUBLIC_RECAPTCHA_ENTERPRISE_SITE_KEY` | Firebase App Check (reCAPTCHA Enterprise site key) |
| `NEXT_PUBLIC_APP_CHECK_DEBUG_TOKEN` | App Check debug token for local dev (`true` to auto-generate, or paste a registered UUID) |
| `NEXT_PUBLIC_GA_MEASUREMENT_ID` | Google Analytics 4 measurement ID (`src/components/GoogleAnalytics.tsx`, `src/lib/gtag.ts`); page load + client-side route change tracking |
| `NEXT_PUBLIC_SITE_URL` | Canonical site URL for metadata, sitemap, and robots.txt; defaults to `https://devfest.gdgsydney.com` |

## Current Status

**Active milestone:** 8 — Speaker & schedule pages. Speakers are done (`/speakers`, `/speakers/[slug]`, landing section, `/admin/speakers` with photo upload); the schedule page is the remaining piece and waits on the `schedule` collection being populated. Also shipped on 2026-09-06 outside the milestone list: `/partners` and a site-wide `not-found.tsx`. Milestone 7 (SEO) is complete for every public page, each with its own metadata, canonical and OG image, and all in the sitemap.
**Public nav as of 2026-09-06:** Speakers, Partners, Builder Showcase, plus the ticket / CfS CTA. Pages only, never anchors into `/`; About, Tracks and Venue are reached from the footer.
**Verified:** `/`, `/call-for-speakers`, and `/conduct` each have correct `<title>`, canonical URL, and page-specific OpenGraph/Twitter metadata. `/call-for-speakers` has its own dynamic OG image (`opengraph-image.tsx`, on-brand GDG dots + wordmark, generated via `next/og`, no stored asset); `/` and `/conduct` share the site-wide default (`src/app/opengraph-image.png`, static since 24 August, served at `/opengraph-image.png` and referenced via `SITE_OG_IMAGE` — note: file-convention OG images do NOT cascade to child routes, so `/conduct` references it explicitly via `openGraph.images`/`twitter.images`). `sitemap.xml` and `robots.txt` (disallows `/admin`, `/api`) added. Minimal `Organization` JSON-LD added to the root layout. Confirmed via curl (200s, correct tags, valid sitemap/robots output) and visually inspected both OG images.
**Date and venue confirmed 2026-08-18:** Saturday, 10 October 2026 at Torrens University, Surry Hills (Shop 1/37 Foveaux St, Surry Hills NSW 2010). Added to `EVENT.md`, footer date/venue line, FAQ answer, root layout metadata/OG copy, and homepage OG image. `Event` JSON-LD added to `/` (`src/app/page.tsx`) now that `startDate` is confirmed — previously deferred pending this.
**Venue section shipped:** the `/` page Venue section is built and live (`showVenue = true` in `src/app/page.tsx`, commit `3d84295 Add venue section to landing page`). Next task #1 below is complete.
**Ticket pivot shipped 2026-08-30 (behind a flag):** with the CfS closing 30 August, the site's primary conversion moves from proposals to ticket sales. `/tickets` page (open + "not on sale yet" states, own metadata/canonical/OG image), a ticket section on `/` (it replaced the CfS callout that sat between About and Tracks, and inherited that slot), and ticket CTAs in the hero, navbar, mobile menu and top banner. The header nav was trimmed to About and Tracks on 2026-08-30 (and on 2026-09-06 to pages only, Speakers and Builder Showcase, since anchors into `/` only scroll the page you are on): `/tickets` is reached from the CTA button next to those links, the landing page's ticket section, and the footer's Event column, so a nav item for it only repeated the CTA beside it. The footer `/tickets` link is permanent, shown whether or not sales are open, since `/tickets` has a "not on sale yet" state; only the CTA buttons are gated on the on-sale date. Note the CTA reads "Apply to speak" while the CfS is open, so until the CfS closes the header has no direct route to `/tickets`. Sales open on a schedule rather than a deploy: `areTicketsOpen()` in `src/lib/tickets.ts` is evaluated server-side per render against `TICKETS_ON_SALE_DATE`. `Navbar` is a client component and takes the result as a prop; `Footer` is a server component and calls the helper directly. Every page carrying a ticket CTA is `force-dynamic`, joining `/call-for-speakers` and `/volunteer`. **This is load-bearing, not a preference:** `revalidate = 60` was tried first and measured NOT to work. A production server built with an on-sale moment 45 seconds out still served the closed page 2 minutes later, and a runtime-only override on the same build showed 4 ticket CTAs on the force-dynamic `/call-for-speakers` while `/` stayed at 0. A prerender freezes the CTA state at build time; only per-request rendering picks the change up. **Humanitix is live as of 2026-09-07:** `https://events.humanitix.com/devfest-sydney` resolves, with General Admission $40, GA + T-Shirt $50, Enterprise $300, and Second Release tiers ($45 / $55) that open when the first release sells out. Booking fees are on top. `TICKETS_ON_SALE_DATE` is set to `2026-08-31T00:00:00+10:00`, matching `CFS_CLOSE_DATE`: tickets go on sale the moment the CfS closes. Tickets stay closed if the Humanitix URL is ever unset, regardless of the date. Before the on-sale moment the landing page simply has no section in that slot, since the CfS callout it replaced is gone. The CfS countdown bar above About is untouched and disappears on its own when `CFS_OPEN` goes false. The `landingCfsImageUrl` field on `settings/site` is no longer read by anything.
**CfS auto-close wired 2026-08-30:** `isCfsOpen()` in `src/lib/cfs.ts` combines `CFS_OPEN` with `CFS_CLOSE_DATE` and is evaluated per request, mirroring `areTicketsOpen()`. `CFS_CLOSE_DATE` and `TICKETS_ON_SALE_DATE` are both `2026-08-31T00:00:00+10:00`, so the CfS closes and ticket sales open at the same instant. Two bugs were found and fixed while wiring it:
- **The close date rendered a day early in production.** `formatCloseDate` used the server's local timezone, and App Hosting runs UTC, so `2026-08-31T00:00:00+10:00` displayed as "30 August". The shared `formatCloseDateTime()` now pins `Australia/Sydney` and includes the time, since a bare day reads as "any time that day" when the deadline is midnight. Verified by running the server under `TZ=UTC`.
- **`/api/submit-proposal` accepted submissions regardless of `CFS_OPEN`.** Hiding the form never closed the endpoint, so a stale tab or direct POST still wrote to `submissions`. It now returns 403 with a plain-English message that `CfsForm` surfaces through the existing `Alert`. `/api/submit-volunteer` had the same gap against `VOLUNTEER_OPEN`; fixed 2026-09-06.

Also removed: a dead `formatCloseDate` in `src/app/page.tsx` (defined, never called), and the module-level `isCfsOpen` in `/call-for-speakers` that froze at process start so the deadline could never pass.

**Event JSON-LD filled out 2026-09-06:** the `/` Event block now lives in `src/lib/eventJsonLd.ts` (`buildEventJsonLd(speakers)`) and carries `startDate` / `endDate` as full datetimes with the +11:00 AEDT offset (10 October is after the daylight-saving switch; matches the Add to calendar link), `image` (the site OG image), `offers` (Humanitix URL, `InStock` while `areTicketsOpen()` else `PreOrder`, `validFrom` from `TICKETS_ON_SALE_DATE`; no price yet) and `performer` (every confirmed speaker as a Person linking to their page). `buildEventReference()` is the slimmer nested Event each speaker page's Person block uses for `performerIn`, sharing the same dates and PostalAddress. `offers.price` / `priceCurrency` (AUD) were added 2026-09-07 from `TICKET_LOWEST_PRICE`, once the Humanitix tiers were published. One Offer at the cheapest tier rather than an `AggregateOffer`: a $40 to $300 range (Enterprise) would misrepresent the general ticket. This is the one place the site restates a Humanitix figure, so it is an env var, not code.
**Builder Showcase call for demos shipped 2026-09-02:** `/builder-showcase` (hero, a "How it works" section, form or closed state, own metadata/canonical/OG image), `/api/submit-showcase` (validation, Firestore write to `showcase`, Resend confirmation), and `/admin/showcase` (accept / reject / restore / archive, reviewer notes, status filter, search). Wired into the admin sidebar, the footer's Support column (via a new `ShowcaseLink`, matching `VolunteerLink`'s localStorage attribution), and the sitemap. `Navbar` gained a `yellow` accent for it: yellow is the Builder Showcase brand colour, and white on `#f9ab00` fails WCAG AA, so that accent pairs with Black 02 text and the text colour moved into `ACCENT_CLASSES` (two competing `text-*` utilities in one class string is decided by stylesheet order, not source order). `formatCloseDateTime` moved from `src/lib/cfs.ts` to `src/lib/format.ts` so both deadlines share the Sydney-pinned rendering. `/api/submit-showcase` closes itself on `isShowcaseOpen()`, so it never had the gap `/api/submit-volunteer` had until 2026-09-06. Verified against a dev server in both states: form + 400 validation messages when open, closed-state page + 403 when `SHOWCASE_OPEN=false`. **Tracking links added 2026-09-07:** `/admin/links` offers `/builder-showcase` and `/volunteer` as destinations alongside the homepage and CfS. The form already captured UTM params via `getTrackingParams()` and `/api/submit-showcase` already stored them, but nothing showed them: each `/admin/showcase` and `/admin/volunteers` card now has a "Link tracking" line (ref / source / medium / campaign) in its expanded details, matching the CfS card. `/admin/analytics` was then split into tabs the same day (see below).

**Analytics tabs 2026-09-07:** `/admin/analytics` has Speakers / Volunteers / Builder Showcase tabs, each with the same shape: status tiles, a cumulative-over-time chart, breakdown cards and a Traffic sources card. Tabs are plain links carrying `?tab=volunteers` / `?tab=showcase` (Speakers is the bare URL, and an unknown value falls back to it), so a view is bookmarkable and the page stays a server component; `page.tsx` fetches all three collections in parallel. Shared tiles, bars and the channel bucketing live in `src/app/admin/analytics/shared.tsx`; the three panels are `SpeakerAnalytics`, `VolunteerAnalytics` and `ShowcaseAnalytics`. `SubmissionsOverTimeChart` now takes any `{ submittedAt }[]` plus a noun and an accent (blue / green / yellow, matching each dashboard's card border). Volunteer breakdowns: areas of interest (multi-select, so percentages are of signups, not of ticks), profile flags, GDG on Campus chapter (of execs only). Showcase breakdowns: stage, team size, entrant profile. Fixed while doing this: the chart's gridline keys collided when a series had fewer than 4 entries (rounding four steps of a max of 3 gave two "2"s), which the CfS series was always too big to hit. Verified all three tabs in the browser against live dev data.

**Speaker acceptance emails shipped 2026-09-03:** built to the "Email template - Confirmed Speaker" frame in the DevFest Figma file. Three pieces:
- `src/lib/acceptanceEmail.ts` - the speaker-facing acceptance email (table-based HTML, escaped speaker input, track/format/level pills, the Humanitix share line only when `NEXT_PUBLIC_HUMANITIX_URL` is set) plus `buildSpeakerConfirmedNotice()`, the plainer notice the organisers get back.
- `sendAcceptanceEmail` in `src/app/admin/actions.ts` - admin-only, accepted-only, records `acceptanceEmailSentAt` / `acceptanceEmailSentBy` / `confirmByDate` and a reviewer note. Unlike the public form endpoints, a send failure is surfaced to the admin rather than swallowed: they are standing right there and need to know the speaker was never told. In the dashboard it is an envelope button in the right action rail above Undo (NOT in the overflow menu), green until it has been used, paired with a Not emailed / Awaiting confirmation / Confirmed chip in the meta row.
- `/speaker/confirm` - a standalone card with the wordmark inside it, deliberately no navbar and no footer, since every link out of this page is a way to leave without answering the one question it asks. The button reuses the shared submit-button class string from `CfsForm` / `VolunteerForm` / `ShowcaseForm`.

`CONFIRM_WINDOW_DAYS` is 8, measured from the send rather than from the acceptance: the speaker's window starts when they can actually read about it. Confirming emails hello@gdgsydney.com with `replyTo` set to the speaker, once, on the transition only.

Verified in dev against temporary accepted submissions (both since deleted): the confirm page renders, confirms, persists as "already confirmed" on reload, and rejects a bogus token; the rail button and chip render in both card and list views. Sample sends of both emails went to an organiser inbox and look right. **No acceptance email has been sent to a real speaker yet** - all 11 accepted submissions are still unemailed.

**Deployment note:** `SPEAKER_CONFIRM_SECRET` was NOT in Secret Manager on the first deploy, and App Hosting failed with "Misconfigured secret ... versions/latest". Adding the block to `apphosting.yaml` does not create the secret. It was created 2026-09-03 with a freshly generated 32-byte value (deliberately NOT the `.env.local` value, so the production signing key has never been in a file on a laptop) and access granted to the `devfest-sydney` backend. Local and production therefore sign with different keys: a confirm link minted on localhost will not validate in production, and vice versa. Rotating this secret invalidates every confirmation link already sitting in a speaker's inbox. Secrets resolve at rollout time, so a redeploy of the same commit is enough to pick it up: `firebase apphosting:rollouts:create devfest-sydney --git-branch main --project devfest-sydney-2026`.

**Contrast pass 2026-09-06:** audited every colour utility against the then `#17181a` page background (changed to `#010103` on 2026-09-07; darker, so every ratio only improved). Fixes: text at `white/30` to `white/45` (2.7 to 4.5:1) raised to `white/50` or `white/55`; core-red text and `red/80` (3.1 to 4.5:1) moved to a new `text-google-red-light` (Material Red 300, 7.6:1); solid brand buttons with white labels moved to new `-deep` fills (Material Blue 600 / Green 700 / Red 600, all at or above 4.5:1 with white, since 16px bold is not "large text" under WCAG); outline buttons and text inputs given `border-white/40` and `border-white/35` boundaries (3:1); the remaining hardcoded greys and blues in class names replaced with tokens. Tokens are documented in `BRANDING.md`. Not changed: `text-google-blue` / `-green` / `-yellow` on the dark page (all pass), and the 11px admin status chips, where blue on a blue tint sits near 4.2:1.

**Admin speakers page shipped 2026-09-06:** `/admin/speakers` lists the `speakers` collection (the promoted lineup, not the proposals). Each card joins back to its source submission via `submissionId` to show the same Not emailed / Awaiting confirmation / Confirmed chip as `/admin`, and flags a "Missing photo, tagline, bio" chip so gaps on the public speakers section are visible before Milestone 8 ships it. Edit opens `EditSpeakerModal` (profile: name, email, tagline, bio, photo URL, links; session: title, abstract, track, format, level), saved through `updateSpeaker` in `src/app/admin/speakerActions.ts`, which re-applies the `isValidSpeaker` limits from `firestore.rules` since the Admin SDK bypasses them. Remove deletes the speaker doc and returns the proposal to pending in one batch, the same as Undo on `/admin`, behind an inline Keep / Remove confirm. Photos are uploaded from the modal (`uploadSpeakerPhoto`, JPEG/PNG/WebP up to 5 MB) to `speaker-photos/<speakerId>/<uuid>.<ext>` in the default bucket, made public, and stored as a `storage.googleapis.com` URL, the same host the site's other assets use and one `next/image` already allows. Uploads save immediately, independent of the Save button; replacing or removing a photo, or removing the speaker, deletes the old object, but only under `speaker-photos/` so hand-pasted shared assets are never touched. `experimental.serverActions.bodySizeLimit` in `next.config.ts` is raised to 6 MB for this, since the default 1 MB would reject most phone photos. Searching covers name, email and talk title; the filter is by track. Verified against the 11 real speakers in dev.

**Public speakers page shipped 2026-09-06 (Milestone 8, part 1):** `/speakers` renders `fetchPublicSpeakers()` from `src/lib/speakers.ts`, which keeps only speakers whose source submission has `speakerConfirmedAt` set and projects them to `PublicSpeaker` (no email, no submission id). A promoted speaker who has not been emailed, or has not answered, is deliberately not announced. Cards are grouped under Developer / Builder / Workshops headings, sorted by name, with photo or initials, tagline, profile links, format label, talk title, and a native `<details>` disclosure for the abstract and bio (keeps the page a server component). Empty lineup shows a "being finalised" state pointing at the tracks. Own metadata, canonical, and OG image (red accent, "Meet the Speakers"); in the sitemap at 0.8; "Speakers" added to the navbar and the footer's Event column. `force-dynamic` like the other public pages. Verified in dev with the 8 confirmed speakers. Landing page section added the same day: `/` shows up to `LANDING_SPEAKER_LIMIT` (8) confirmed speakers as avatar tiles between Tracks and Venue, each linking to `/speakers#<track>`, with a "See all N speakers" / "See the full lineup" button; hidden entirely until someone has confirmed. Individual speaker pages added the same day: `/speakers/[slug]`, where the slug is `toSpeakerSlug(name)` from `src/lib/speakers.ts` ("Brett Morgan" to `brett-morgan`), with `-2`, `-3` suffixes if two confirmed speakers share a name. Slugs are computed per request from the confirmed list rather than stored, so renaming a speaker in `/admin/speakers` changes their URL; nothing links to the old one except whatever was shared externally. `fetchPublicSpeakerBySlug()` returns null for both unknown and unconfirmed speakers, and the page calls `notFound()` for either. Each page has hero (photo, track, name, tagline, profile links), the session in a red-bordered card, the bio, a sticky ticket aside (Humanitix CTA via `TicketsLink` with `source="speaker-<slug>"` when on sale, otherwise a link to `/tickets`), and four other speakers. `generateMetadata` gives each its own title, description, canonical and a per-speaker OG image (name + talk title; an unknown slug gets the generic lineup card). Person JSON-LD with `performerIn` the event. Every confirmed speaker is in the sitemap at 0.6. Cards on `/speakers` and tiles on `/` link through. The LinkedIn / GitHub / website icons moved to `src/components/SocialIcons.tsx` so the two pages share them. A site-wide `src/app/not-found.tsx` was added at the same time, since a stale speaker link was the first route to land on Next's bare default 404. NOT yet done for Milestone 8: the schedule page.

**Partners page shipped 2026-09-06:** `/partners` is the sponsorship pitch, with copy and figures lifted from the prospectus PDF in Storage (`sponsorship/DevFest-Sydney-Sponsorship-Prospectus.pdf`). Sections: hero with prospectus download and a `mailto:hello@gdgsydney.com` CTA; "The partners behind the day" (Google as presenting partner, Torrens as venue partner); sponsor logos grouped by tier from the `sponsors` collection, with a "being confirmed" state while the collection is empty (it was until 2026-09-10; see the sponsor note below); closing CTA ("Tiers, benefits and how to get involved") with an "Email us" mailto button and the prospectus download. Tier pricing, benefits and the why-sponsor pitch are deliberately NOT on the page: they live only in the prospectus so there is one place to keep current (tier cards and a why-sponsor section were built and then removed the same day at the user's request). Own metadata, canonical and OG image (green accent, "Partner with DevFest"); in the sitemap at 0.6; "Partners" added to the navbar (Speakers, Partners, Builder Showcase) and the footer's Event column now points at the page rather than the landing anchor. Sponsor fetching, tier constants and the settings/site asset reads (prospectus, Google and Torrens logos) moved to `src/lib/sponsors.ts` and are shared by `/`, `/partners` and `/faq`. The landing page's "Supported by" strip and the hidden `showSponsors` section were merged into one `#partners` section: the two logos, sponsor tiers when the collection has entries, and a "Become a partner" link (reads "All partners and how to join them" once sponsors exist). Verified in dev.

**SEO audit 2026-09-06:** crawled every public route plus admin, confirm and 404. All public pages already had unique titles, descriptions, canonicals, OG images, one H1, alt text on every image and valid JSON-LD. Six fixes made: (1) per-page `openGraph` objects replace the root one rather than merging, so every subpage had lost `og:site_name` and `og:locale`; all public page metadata now goes through `buildPageMetadata()` in `src/lib/metadata.ts`, which sets the full set. (2) Admin routes were indexable (robots.txt only blocks crawling) and titles were doubling the site suffix; `src/app/admin/layout.tsx` now sets `robots: noindex, nofollow` and a `%s · Admin — DevFest Sydney 2026` template, with page titles reduced to bare names and the login title `absolute`. (3) Root description trimmed from 252 to 160 characters (it is also the fallback for the 404, confirm and admin pages). (4) `lastModified: new Date()` removed from the sitemap; a date that is always "now" is noise. (5) `src/app/favicon.ico` added (32px PNG-in-ICO from `icon.png`) since `/favicon.ico` was 404 and Next only emits the `icon.png` link. (6) Dead `preconnect` hints to Google Fonts removed from the root layout; fonts are local. Left as-is, deliberately: FAQ questions are buttons without heading markup (FAQPage rich results are restricted to government and health sites since 2023, so no JSON-LD added), the landing tagline under the H1 is an H2, `lang="en"` rather than `en-AU`, no ItemList on `/speakers`. Core Web Vitals not measured: run Lighthouse against the production URL after deploy.

**Single OG image 2026-09-07:** every page now shares the site-wide `src/app/opengraph-image.png`. The per-page `opengraph-image.tsx` routes (`/builder-showcase`, `/partners`, `/speakers`, `/speakers/[slug]`, `/tickets`, `/volunteer`) and the static PNGs for `/call-for-speakers` and `/admin` were deleted; `buildPageMetadata()` always sets `images: [SITE_OG_IMAGE]` and no longer takes an `images` argument, and the admin pages reference `SITE_OG_IMAGE` directly. Any mention below of a page-specific OG image is historical. Verified with curl on the dev server: every public page, a speaker page and `/admin/login` emit `og:image` pointing at `/opengraph-image.png` (200).

**Stale OG image references fixed 2026-09-07:** when the landing OG image became a static PNG (`2e9d9c6`, 24 August), its URL changed from `/opengraph-image` to `/opengraph-image.png`, and `/conduct`, `/privacy`, `/faq` and the Event JSON-LD `image` kept pointing at the old path, which 404'd. All four now use `SITE_OG_IMAGE` from `src/lib/metadata.ts`. Verified on the dev server.

**Volunteer endpoint closed 2026-09-06:** `/api/submit-volunteer` now returns 403 with a plain-English message when `isVolunteerOpen()` is false, matching the proposal and showcase endpoints, and `VolunteerForm` already surfaces that message through the alert. `isVolunteerOpen()` lives in `src/lib/volunteer.ts` and is the single source for the page, the sitemap and the endpoint; it is a function rather than a constant so a close date can be added later the way the CfS and showcase have one. Verified on a throwaway server with `VOLUNTEER_OPEN=false`: 403 from the endpoint, closed state on the page, sitemap priority 0.3. All three public form endpoints now close with their forms.

**Admin performance pass 2026-09-07:** three changes, all measured. (1) Every admin page load and every server action called `verifySessionCookie(cookie, true)`; the `true` (check revoked) is a round trip to the Identity Toolkit API measured at 0.5 to 1.2 s from Sydney, on top of the Firestore reads. Dropped: the `admins` document check already locks out a removed admin on their next request, and the cookie expires in 5 days. The four copies of `verifyAdminSession()` in the action files now import one from `src/lib/adminSession.ts`, wrapped in React `cache()`. (2) `AdminShell` imported `@/lib/firebase` for the sign-out button, which pulled the whole client SDK (Auth, Firestore, Storage, App Check and its reCAPTCHA script) into every admin page: 220 to 231 kB First Load JS against 115 kB for `/`. The import is now dynamic inside `handleSignOut`; admin pages are 105 to 116 kB. `/admin/login` is unchanged at 228 kB since it genuinely needs Firebase Auth. (3) The dashboard pages moved into a `src/app/admin/(dashboard)/` route group whose `layout.tsx` renders `AdminShell` once, so the sidebar stays on screen between sections and `loading.tsx` shows a skeleton inside it while the next page fetches. Login stays outside the group. Pages fetch their collection in parallel with the session check. Components, actions and modals stay in `src/app/admin/`. **Sidebar streamlined the same day:** the account popover at the foot of the sidebar (profile, Invite admin, Sign out behind a toggle) is gone; the foot is now a flat row of avatar, name, email and an inline sign-out icon button. "Invite admin" moved to a button in the `/admin/admins` header, which owns `InviteAdminForm` now, and `addAdmin` revalidates `/admin/admins` rather than `/admin`. The desktop sidebar and the mobile menu share one `NavLinks` and one `AccountRow`, so the two no longer carry duplicate markup; the hamburger toggles to a close icon while the menu is open. Nav items sit under four mono-label groups: Review (Submissions, Volunteers, Showcase), Content (Speakers, since it manages the public lineup rather than proposals; the schedule page's admin will join it), Insights (Analytics, Links) and Settings (Admins), defined once in `NAV_GROUPS`. Note for local dev: `npm run build` and `next dev` share `.next`, so a production build while the dev server runs breaks it until restarted.

**Speaker links and slug redirects 2026-09-07:** profile links come from the CfS form, which never required a scheme, so three speakers (one confirmed) had `linkedin.com/in/...` values that rendered as relative links under `/speakers/`. `normaliseProfileUrl()` in `src/lib/speakers.ts` now adds `https://` on the way out (`fetchSpeakers`) and on the way in (`promoteSubmission`, `updateSpeaker`, which rejects anything still not a web address with a plain message). The four affected documents were corrected in Firestore the same day; every other link was HEAD-checked and resolves (LinkedIn answers 405 to HEAD, which only proves the host, not the profile). Renaming a speaker in `/admin/speakers` now records the old slug in `previousSlugs` on the speaker doc, and `/speakers/[slug]` issues a 308 to the current slug for any remembered one (`findCurrentSlugForPreviousSlug`); a live slug always wins, only confirmed speakers redirect, and a name changed back drops the slug from the history. `firestore.rules` allows the new optional `previousSlugs` list and needs a rules deploy. Verified in dev: redirect, no-hijack of a live slug, 404 for unknown.

**First sponsor and sponsor blurbs 2026-09-10:** Aviato Consulting is the first entry in the `sponsors` collection (doc `aviato-consulting`, Gold, `https://aviato.consulting`). The logo lives in Storage at `sponsor-logos/aviato-consulting.png`, trimmed of its transparent padding and downsized from the 13,696px source to 1,600px wide (118 KB). It is a white wordmark on transparent, so it only reads on the dark page; it is also a stacked lockup, so it sits small at the tier height caps. Sponsors gained an optional `description` (string, up to 600 chars; type in `src/lib/types.ts`, rules deployed the same day). On `/partners`, each tier now renders a centred, wrapping row of cards (logo, "<Tier> sponsor" label, blurb, "Visit <name>" link, half width on `md` and up) for sponsors that have a description, then the plain logo row for those that don't, so a sponsor never appears twice. The landing page strip stays logos only. The sponsor doc is written with `website` and `order`, which is what `fetchSponsors()` and the pages read; `firestore.rules` still documents `websiteUrl` and no `order`. That mismatch only matters for client writes, which nothing does, and is unfixed. Note for local dev: on 2026-09-10 two `next dev` processes were found sharing `.next` (one from a Terminal tab, one from an earlier session) and the page served with a 404 stylesheet until both were stopped and one restarted; check `pgrep -fl next` if CSS goes missing.

**Asana added as Silver sponsor 2026-09-11:** doc `sponsors/asana` (Silver, `https://asana.com`, order 2, with a 366-character description written by Claude rather than supplied by Asana, so it renders as a blurb card like Aviato). No official reversed logo pack was reachable without a form, so the logo at `sponsor-logos/asana.png` (1,600px wide, 31 KB, public) was rendered from the Asana wordmark SVG with the black wordmark set to white and the coral dots unchanged, which matches Asana's reversed lockup for dark backgrounds. Verified on `/partners` and the landing strip in dev. **Logo boxes:** with a shared height cap the stacked Aviato lockup rendered a third the width of the Asana wordmark, so every sponsor logo now sits in a fixed box with both a height and a width cap (`object-contain`, left-aligned in cards, centred in rows): card `h-20 w-48` (Platinum `h-24 w-56`), logo row `h-14 w-36` (Platinum `h-16 w-44`), landing strip `h-12 w-32` (Platinum `h-14 w-40`). A stacked logo fills the height, a wide one the width, so their visual weight is close. Note for local dev: two `next dev` processes were again sharing `.next` this session (port 3000 answered 404 then 500, 3001 was fine); verification used 3001.

**Core-X added as Community sponsor 2026-09-11:** doc `sponsors/core-x` (Community tier, `https://core-x.ai`, order 3, no description, so it shows as a logo under a "Community" tier heading rather than a card). Logo from `~/Downloads/Core-X_Logo_White.png`, trimmed of its transparent padding (1,500px square to 1,105 by 299) and uploaded to `sponsor-logos/core-x.png`, public. The website was not supplied; core-x.ai serves the identical logo file name, which is how it was confirmed. Note this is a community *sponsor* in `sponsors`, not a community *partner* in `partners`.

**Community partners section 2026-09-11:** `/partners` has a "Community partners" section (`#community`) between Sponsors and the closing CTA, fed by a new `partners` Firestore collection (`fetchCommunityPartners()` in `src/lib/sponsors.ts`). The distinction: a sponsor gives something (money or in-kind, every tier including `community`, listed by tier in `sponsors`); a community partner is a meetup, student group or developer community that promotes DevFest to its members in a reciprocal, unpaid arrangement. The two collections share one shape, `PartnerOrganisation` in `src/lib/types.ts` (name, logoUrl, website, order, optional description), with `Sponsor` adding `tier`. Rules for `partners` mirror `sponsors` (public read, admin-only write, `isValidCommunityPartner`), and the `sponsors` rule was corrected at the same time to the fields actually written (`website` and `order`, not `websiteUrl`), closing the mismatch noted on 2026-09-10; both validate through a shared `isValidOrganisation`. Rules deployed 2026-09-11. Same layout as a sponsor tier: blurb cards labelled "Community partner" for entries with a `description`, a logo row for the rest, a "being confirmed" state with a mailto while empty (it is empty as of today). The card, logo link and group markup are `SponsorCard`, `SponsorLogoLink` and `PartnerGroup` inside `src/app/partners/page.tsx`, shared by both sections. The landing page strip shows sponsors only; community partners appear nowhere but `/partners`. Verified both states in dev, the populated one with a throwaway doc since deleted.

**Next task:**
1. Milestone 8 — the schedule page, once the `schedule` collection is populated.

Otherwise: Milestone 9 — Polish & launch.
